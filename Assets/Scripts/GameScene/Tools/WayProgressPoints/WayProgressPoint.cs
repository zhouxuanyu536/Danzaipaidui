using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum WayProgressType
{
    Horizontal,
    Vertical
}
public class WayProgressPoint : MonoBehaviour
{
    public float OnPointLineProgress;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
